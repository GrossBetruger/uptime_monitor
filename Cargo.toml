[package]
name = "uptime_monitor"
version = "0.1.0"
edition = "2024"


[dependencies]
# HTTPS-capable, simple blocking client with JSON
reqwest = { version = "0.11", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }


